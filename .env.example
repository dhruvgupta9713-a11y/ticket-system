# Port the server will listen on
PORT=8080

# Secret key used to sign and verify JWT tokens
JWT_SECRET=replace_with_your_secure_secret_key_in_production

# Path to the SQLite database file
DB_PATH=tickets.db
